name: standardize yaml
on: [pull_request, push]
jobs:
  check-yml-files:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: gentx-validation
        run: |
          if [[ $(git ls-files '*.yml'| wc -l) -ne 0 ]]; then exit 1;fi

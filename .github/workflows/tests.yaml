name: tests

on:
  pull_request:
  push:
    branches:
      - master
      - mainnet/main

env:
  MAINNET: ${{ github.ref == 'refs/heads/mainnet/main' || github.base_ref == 'refs/heads/mainnet/main' }}

jobs:

  build-bins:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-go@v2
        with:
          go-version: ${{ secrets.GOLANG_VERSION }}
      - run: make bins
      - run: make docker-image

  tests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-go@v2
        with:
          go-version: ${{ secrets.GOLANG_VERSION }}
      - run: make test-full

  coverage:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-go@v2
        with:
          go-version: ${{ secrets.GOLANG_VERSION }}
      - run: make test-coverage
      - uses: codecov/codecov-action@v1

  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-go@v2
        with:
          go-version: ${{ secrets.GOLANG_VERSION }}
      - run: make build
      - run: make test-vet
      - run: make lintdeps-install
      - name: lint all
        run: $(go env GOPATH)/bin/golangci-lint run ./... --issues-exit-code=0 --deadline=10m
      - name: lint make-sublinters
        run: make test-sublinters 
